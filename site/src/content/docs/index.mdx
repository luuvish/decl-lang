---
title: Decl
description: A declarative language for describing, generating, and validating structured data — a JSON superset with types, constraints, references, quantities, generics, and modules.
template: splash
hero:
  tagline: Describe structured data, generate values from the description, and validate documents against it. A JSON superset with a strong static type system, constraints with first-class diagnostics, references, physical quantities, generics, and modules. Pure, deterministic, terminating.
  actions:
    - text: Decl by example
      link: /guide/01_overview_by_example/
      icon: right-arrow
    - text: Try it in the browser
      link: /playground/
      icon: rocket
      variant: secondary
    - text: Install
      link: /start/install/
      variant: minimal
  image:
    html: '<svg viewBox="6 5 28 30" role="img" aria-label="The subsumption sign, value ⊑ type"><path fill="currentColor" d="M8 7h24v4H12v10h20v4H8zM8 29h24v4H8z"/></svg>'
---

import { Card, CardGrid, Code, Tabs, TabItem, LinkCard } from '@astrojs/starlight/components';
import hero from '../../samples/hero.decl?raw';
import heroOut from '../../samples/synced/hero.json?raw';

<div class="three">
<CardGrid>
  <Card title="Describe" icon="document">
    Types, defaults, derived members, and constraints live in one flat schema. No domain keywords — graphs, configs, and hardware are all just records, references, and rules.
  </Card>
  <Card title="Generate" icon="setting">
    Comprehensions and derivations turn a description into canonical JSON, byte-identical on every run and every implementation — and, with `@render`, into YAML, indented JSON, or text from a template, one file per element.
  </Card>
  <Card title="Validate" icon="approve-check">
    Bind external documents to the same types. Every failure is a diagnostic with a path, a code, and a root cause — never a cascade.
  </Card>
</CardGrid>
</div>

## One file, three capabilities

<div class="two-col">
  <Code code={hero} lang="decl" title="services.decl" />
  <Code code={heroOut} lang="json" title="decl evaluate services.decl" />
</div>

The same `Service` type that generated `demo` validates `deployed` documents from the outside world:

```bash
decl validate services.decl --input deployed=prod.json
```

## Install

<Tabs syncKey="channel">
  <TabItem label="npm">
    ```bash
    npm install -g decl-lang
    ```
    The TypeScript reference implementation: `decl` and the `decl-lsp` language server (Node.js ≥ 22).
  </TabItem>
  <TabItem label="pip">
    ```bash
    pip install decl-lang
    ```
    The native Python implementation: `decl`, `decl-lsp`, and a Python API — no Node.js.
  </TabItem>
  <TabItem label="cargo">
    ```bash
    cargo install decl-lang
    ```
    The native Rust implementation: `decl` and `decl-lsp` with no Node or wasm.
  </TabItem>
  <TabItem label="brew">
    ```bash
    brew install luuvish/tap/decl-lang
    ```
    The npm package through a Homebrew tap.
  </TabItem>
</Tabs>

## Tools

The same language server, formatter, and evaluator in every implementation; the editor extensions and the REPL are clients of them.

```bash
decl check services.decl                     # the static checks, and nothing else
decl fmt --check services.decl               # the canonical form
decl repl services.decl                      # a session over the module: expressions, :type, :trace, undo
decl-lsp --stdio                             # what VS Code, Zed, Neovim, Helix, and Emacs talk to
```

<CardGrid>
  <LinkCard title="Command line" href="/tooling/01_cli/" description="check, evaluate, validate, fmt — universes, bound inputs, the --json report, exit codes." />
  <LinkCard title="REPL" href="/tooling/02_repl/" description="A session over the evaluation universe: partial evaluation, session outputs, document edits with exact undo." />
  <LinkCard title="Language server" href="/tooling/03_lsp/" description="Diagnostics, hover, completion, navigation, rename, code actions, semantic tokens — the same answers from all three servers." />
  <LinkCard title="Editor extensions" href="/tooling/04_extension/" description="VS Code and Zed extensions; configurations for Neovim, Helix, Emacs, Vim, and Sublime." />
</CardGrid>

## Read on

<CardGrid>
  <LinkCard title="Decl by example" href="/guide/01_overview_by_example/" description="One scenario end to end — describe, generate, validate — with the JSON and diagnostics it produces." />
  <LinkCard title="Specification" href="/specification/01_introduction/" description="The normative specification: types, expressions, constraints, modules, semantics, grammar, errors, and the standard library." />
  <LinkCard title="Examples" href="/examples/" description="A hardware interconnect, an API config schema, and a fixture sweep — written with no domain keywords." />
  <LinkCard title="Design" href="/design/00_vision/" description="Why the language is shaped this way: vision, requirements, and the decision charter." />
</CardGrid>
