const count = services |> std.array.filter((s) => s.public) |> std.array.count
const acc = std.array.fold(xs, 0, (a, x) => a + x)
const prod = base with { workers: 32, tls: base.tls with { on: true } }
