// @expect-phase: checking
// @expect-error: E4090
// @expect-message: gives $key no meaning
// a direct member is not a collection element: $key means nothing there (§7.3)
type Port = {
    $key: string
    name = $key
}
type Node = { p: Port }
output n: Node = { p: { } }
