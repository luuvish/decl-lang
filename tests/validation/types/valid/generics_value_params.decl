type Pair<T> = { first: T, second: T }
type Vec<T, N: int> = T[N]
type Bounded<T, N: 1..1024> = { items: T[0..N] }
const M = 8
type PortPair = Pair<1..65535>
type Quad = Vec<int, 4>
type Oct = Vec<int, M>
type Small = Bounded<string, 16>
