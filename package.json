{
  "name": "decl-lang-monorepo",
  "private": true,
  "description": "Decl — one language, three implementations (decl-typescript, decl-rust, decl-python), the shared grammar and conformance corpus, and the website",
  "workspaces": [
    "decl-typescript",
    "tree-sitter-decl",
    "site"
  ],
  "scripts": {
    "build": "npm run build -w decl-lang",
    "test": "npm test -w decl-lang",
    "site": "npm run build -w site",
    "verify": "make verify"
  }
}
