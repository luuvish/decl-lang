{
  "name": "decl-lang-monorepo",
  "private": true,
  "description": "Decl \u2014 one language, three implementations (decl-ts, decl-rs, decl-py), the shared grammar and conformance corpus, and the website",
  "workspaces": [
    "decl-ts",
    "tree-sitter-decl",
    "site",
    "extension/vscode"
  ],
  "scripts": {
    "build": "npm run build -w decl-lang",
    "test": "npm test -w decl-lang",
    "site": "npm run build -w site",
    "verify": "make verify"
  }
}
